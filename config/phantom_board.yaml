phantom_board:
  ros__parameters:
    # Empty means discover by Phantom service UUID.
    address: ""
    scan_timeout: 15.0
    auto_connect: true

    # Observed values: silence, slow, medium, fast, blitz.
    # K9 deliberately uses Fast.
    movement_speed: "fast"
