#!/usr/bin/env bash
set -e

PYTHON="${K9_PYTHON:-$(command -v python3)}"

if [[ -z "$PYTHON" ]]; then
  echo "ERROR: python3 not found on PATH and K9_PYTHON is not set" >&2
  exit 127
fi

exec "$PYTHON" -m k9_chess_pkg.check_setup "$@"
